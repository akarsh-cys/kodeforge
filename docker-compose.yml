version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Backend/Dockerfile
    ports:
      - "4001:4001"
    environment:
      - AWS_REGION=ap-south-1
      - BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0
      - OUTPUT_DIR=./outputs
      # Add your AWS credentials here or use a credentials file/IAM role
      # - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      # - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
    volumes:
      - ./outputs:/app/outputs
    restart: always

  frontend:
    build:
      context: .
      dockerfile: Frontend/Dockerfile
      args:
        - VITE_API_BASE=http://localhost:4001
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always


